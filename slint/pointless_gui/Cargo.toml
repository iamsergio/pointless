[package]
name = "pointless_gui"
version = "0.1.0"
edition = "2021"

[dependencies]
pointless_core = { path = "../pointless_core" }
slint = "1"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }

[build-dependencies]
slint-build = "1"
