- iOS GH CI
- Read from apple calendar
- Style task menu
- Add login wall
- Make login async

- Nice to haves:
  - Use std::expected everywhere
  - return views so we dont allocate
  - nest expecteds
